# .github

This repository holds the organisation profile that GitHub renders at https://github.com/unified-runtimes. The page itself is `profile/README.md` and the mark beside it is `profile/logo.svg`. Community health files and issue templates that should apply to every repository also belong here, since GitHub falls back to this repository when a repository has none of its own.
